# Axon3 — Claude context

## Stack
Markdown, Claude Code skills, GitHub Actions YAML

## Key commands
None — no build step

## Kanban labels
- `backlog` — not yet scheduled
- `build-lane` — approved for implementation
- `in-progress` — currently being built
- `done` — merged and closed

## Branch naming
`claude/epic-{number}-{short-title}`

## Axon3 commands
All three commands (`/ax-prep`, `/ax-build`, `/ax-init`) are available in this session via the repo's own `.claude/commands/` directory.
