# [Project name] — Claude context

## Stack
[Tech stack, one line each]

## Key commands
- Test: [command]
- Lint: [command]
- Build: [command]
- Deploy: handled by GitHub Actions on PR merge

## Kanban labels
- `backlog` — not yet scheduled
- `build-lane` — approved for implementation
- `in-progress` — currently being built
- `done` — merged and closed

## Branch naming
`claude/epic-{number}-{short-title}`

## Axon3 skills
Loaded via submodule at `.claude/skills/axon3/`. Update with:
`git submodule update --remote .claude/skills/axon3`

## Constraints
[Paste from SPEC.md Constraints zone]
